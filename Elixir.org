#+PROPERTY: header-args:emacs-lisp :tangle ./conf/elixir.el :mkdirp yes

* Elixir

** Elixir Mode

#+begin_src emacs-lisp
  (use-package elixir-mode)
#+end_src

** Mix

#+begin_src emacs-lisp
  (use-package mix)
#+end_src
